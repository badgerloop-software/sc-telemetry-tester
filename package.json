{
  "name": "sc-telemetry-tester",
  "version": "2.0.0",
  "description": "Telemetry simulator and CSV/Excel replay into InfluxDB — simulates Raspberry Pi CAN bus → Influx",
  "main": "src/replay.js",
  "scripts": {
    "start": "node src/replay.js --realtime --loop",
    "batch": "node src/replay.js",
    "replay": "node src/replay.js",
    "generate": "node src/generate_test_data.js",
    "test": "node src/test_connection.js"
  },
  "keywords": [
    "solar-car",
    "telemetry",
    "influxdb",
    "can-bus",
    "simulator",
    "replay"
  ],
  "author": "Badgerloop Software",
  "license": "MIT",
  "dependencies": {
    "xlsx": "^0.18.5"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
